================================================================================
  LLaMA-Factory 样本级损失加权 — 完整数据流
  追踪 loss_weight 字段从原始 JSON 到损失计算的完整传递路径（共 6 个修改点）
================================================================================


┌──────────────────────────────────────────────────────────────────────────────────────┐
│  Phase 1 · 训练入口                                                                  │
│  [workflow.py]                                                                       │
├──────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  [1] run_sft()                                                                       │
│      SFT 训练工作流入口，串联数据加载、Trainer 初始化、训练执行                       │
│                                                                                      │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
                      调用 get_dataset() 加载数据


┌──────────────────────────────────────────────────────────────────────────────────────┐
│  Phase 2 · 数据准备：解析配置与注册字段                                              │
│  [parser.py]  ★ 修改点 1                                                            │
├──────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  [2] get_dataset() → _get_merged_dataset()                                           │
│      顶层入口，触发数据集加载与融合                                                   │
│                                                                                      │
│  [3] _get_merged_dataset() → get_dataset_list()                                      │
│      读取 dataset_info.json，为每个数据集创建 DatasetAttr 对象                        │
│                                                                                      │
│  [4] DatasetAttr                                                                     │
│      数据集属性数据类，通过 set_attr() 解析 columns 配置                              │
│      ★ 修改点 1: 增加 loss_weight 属性                                               │
│                                                                                      │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
                          逐个加载数据集文件


┌──────────────────────────────────────────────────────────────────────────────────────┐
│  Phase 3 · 数据加载与格式对齐                                                        │
│  [loader.py / aligner.py]  ★ 修改点 2                                               │
├──────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  [5] _load_single_dataset()                                                          │
│      使用 HF load_dataset() 加载原始 JSON 文件                                        │
│                                                                                      │
│  [6] _load_single_dataset() → align_dataset()                                        │
│      根据 formatting 选择转换函数                                                    │
│                                                                                      │
│  [7] align_dataset() → convert_sharegpt()                                            │
│      将 sharegpt 格式转为标准格式（_prompt, _response, _system ...）                  │
│      ★ 修改点 2: 输出 _loss_weight 字段                                              │
│                                                                                      │
│  [8] merge_dataset()                                                                 │
│      将多个对齐后的数据集融合为一个                                                   │
│                                                                                      │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
                  对融合数据集进行 Tokenization


┌──────────────────────────────────────────────────────────────────────────────────────┐
│  Phase 4 · 数据预处理：Tokenization 与权重传递                                       │
│  [loader.py / preprocess.py / supervised.py]  ★ 修改点 3 & 4                        │
├──────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  [9] _get_preprocessed_dataset()                                                     │
│      预处理入口，通过 dataset.map() 批量处理                                         │
│                                                                                      │
│  [10] get_preprocess_and_print_func()                                                │
│       根据 stage 和 packing 配置路由到对应预处理函数                                  │
│                                                                                      │
│       ┌─ 非打包模式 ──────────────────────────────────────────┐                      │
│       │  preprocess_supervised_dataset()                       │                      │
│       │      → _encode_supervised_example()                    │                      │
│       │  ★ 修改点 3: 追加 loss_weight 到 model_inputs          │                      │
│       └────────────────────────────────────────────────────────┘                      │
│                                                                                      │
│       ┌─ 打包模式 ────────────────────────────────────────────┐                      │
│       │  preprocess_packed_supervised_dataset()                 │                      │
│       │      → _encode_supervised_example()                    │                      │
│       │      → greedy_knapsack()                               │                      │
│       │  ★ 修改点 4: 拼接 packed_loss_weight                   │                      │
│       └────────────────────────────────────────────────────────┘                      │
│                                                                                      │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
                        构建 DataLoader 送入训练


┌──────────────────────────────────────────────────────────────────────────────────────┐
│  Phase 5 · DataLoader 构建与列过滤                                                  │
│  [trainer.py (Trainer)]  ★ 修改点 5                                                 │
├──────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  [11] CustomSeq2SeqTrainer.__init__()                                                │
│       初始化 Trainer，接收预处理后的 dataset_module                                   │
│                                                                                      │
│  [12] get_train_dataloader() → _remove_unused_columns()                              │
│       过滤不在模型 forward 签名中的列                                                 │
│                                                                                      │
│  [13] _set_signature_columns_if_needed()                                             │
│       通过 inspect.signature 获取模型 forward 参数列表                                │
│       ★ 修改点 5: 追加 "loss_weight" 到签名列                                        │
│                                                                                      │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
                              进入训练循环


┌──────────────────────────────────────────────────────────────────────────────────────┐
│  Phase 6 · 训练执行：前向传播与损失计算                                              │
│  [trainer.py (Trainer)]  ★ 修改点 6                                                 │
├──────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  [14] trainer.train() → _inner_training_loop()                                       │
│       启动训练，进入内层循环                                                         │
│                                                                                      │
│  [15] training_step() → _prepare_inputs() → _prepare_input()                         │
│       单步训练：将输入张量移至 GPU                                                    │
│                                                                                      │
│  [16] CustomSeq2SeqTrainer.compute_loss()                                            │
│       重写的损失计算方法，注入 loss_weight                                            │
│       ★ 修改点 6: 实现加权损失                                                       │
│                                                                                      │
│  [17] label_smoother_weighted() → model.forward()                                    │
│       加权标签平滑 + 模型前向传播，输出加权损失值                                     │
│                                                                                      │
└──────────────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        ▼
                        梯度累积 / 多 GPU 同步


┌──────────────────────────────────────────────────────────────────────────────────────┐
│  Phase 7 · 梯度累积与多 GPU 行为                                                     │
├──────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                      │
│  [18] loss.backward()                                                                │
│       反向传播，梯度累积至 gradient_accumulation_steps 步后执行 optimizer.step()       │
│                                                                                      │
│  [19] accelerator.backward()                                                         │
│       多 GPU 场景下由 Accelerate 库处理梯度同步（DDP / DeepSpeed）                    │
│                                                                                      │
└──────────────────────────────────────────────────────────────────────────────────────┘


================================================================================
  阶段分组 · 图例
================================================================================

  function()               函数名称
  修改点 N                 需要修改的代码位置
  file.py                  所在源文件
  →                        调用关系
  ★ 修改点 N: ...          具体修改内容说明

